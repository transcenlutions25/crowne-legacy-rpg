# Verified Baseline Import Plan

The complete verified Crowne Legacy Mobile package is staged outside `main`, reconstructed by GitHub Actions, validated with the existing syntax and gameplay test suite, then committed as one baseline import. Temporary package fragments are removed automatically after reconstruction.

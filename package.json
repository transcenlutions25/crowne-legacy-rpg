{
  "name": "crowne-legacy-mobile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Android-first playable vertical slice of Crowne Legacy: The Blackout Contract",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check app.js && node --check game-engine.js && node --check story.js && node --check crown-network.js && node --check server.mjs && node --check sw.js",
    "serve": "node server.mjs"
  }
}
